# checks all networks we care about (kusama, kvp, westend) and ensures
# the bootnodes in their respective chainspecs are contactable

name: Check all bootnodes
on:
  push:
    branches:
      # Catches v1.2.3 and v1.2.3-rc1
      - release-v[0-9]+.[0-9]+.[0-9]+*

jobs:
  check_bootnodes:
    strategy:
      fail-fast: false
      matrix:
        runtime: [westend, kusama, kvp]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v3
      - name: Install kvp
        shell: bash
        run: |
          curl -L "$(curl -s https://api.github.com/repos/paritytech/kvp/releases/latest \
          | jq -r '.assets | .[] | select(.name == "kvp").browser_download_url')" \
          | sudo tee /usr/local/bin/kvp > /dev/null
          sudo chmod +x /usr/local/bin/kvp
          kvp --version
      - name: Check ${{ matrix.runtime }} bootnodes
        shell: bash
        run: scripts/ci/github/check_bootnodes.sh node/service/chain-specs/${{ matrix.runtime }}.json
